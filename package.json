{
  "name": "zlib-test",
  "version": "1.0.0",
  "private": true,
  "description": "test",
  "main": "index.js",
  "scripts": {
    "test": "rm -rf output && rm -rf open-abap && git clone https://github.com/open-abap/open-abap && cp open-abap/src/* src/ && abap_transpile && echo RUNNING && node output/index.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@abaplint/runtime": "^0.5.28",
    "@abaplint/transpiler-cli": "^0.5.28"
  }
}
